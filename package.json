{
  "name": "chat-app",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "yarn --cwd server server",
    "client": "yarn --cwd client start",
    "server": "yarn --cwd server start",
    "dev": "concurrently \"yarn run client\" \"yarn run server\"",
    "build": "yarn --cwd client install && yarn --cwd client build && yarn --cwd server install"
  },
  "author": "Siddharth Roy <siddharthroy36912@gmail.com>",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^5.3.0"
  }
}
